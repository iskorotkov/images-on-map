import { TextStyle, ViewStyle } from 'react-native'

export const defaultStyles = {
  button: {
    paddingVertical: 10,
    paddingHorizontal: 40,
    margin: 10,
    borderRadius: 100
  } as ViewStyle,
  buttonText: {
    color: '#fff',
    textAlign: 'center'
  } as TextStyle
}
